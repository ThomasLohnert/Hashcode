def sort(photos):
    """
    Takes a list of photos (which may be paired) and sorts them
    :param photos: list[(),()] [(idn, orientation, num_of_tags, tags), (idn, orientation, num_of_tags, tags)]
    :return: list[(),()] [(idn, orientation, num_of_tags, tags), (idn, orientation, num_of_tags, tags)]
    """
    return photos